import { findBookDetailListByPageRequest, deleteAddressBook, reValid } from '@/api/email-marketing';
export const mixinsRequest = {
    methods: {
        findBookDetailListByPageRequest,
        deleteAddressBook,
        reValid
    }
};
